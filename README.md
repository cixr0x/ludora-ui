
  # Ludora UI

  Initial Ludora consumer UI, implemented from the supplied Figma export and screenshot references.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
